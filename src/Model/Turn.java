package model;

/**
 * Represents the possible turn states in the game.
 * The game alternates between BLACK and WHITE turns, denoting which player's move is next.
 */
public enum Turn {
  BLACK, WHITE
}